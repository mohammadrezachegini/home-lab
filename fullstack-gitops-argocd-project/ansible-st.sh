mkdir -p ansible/inventory
mkdir -p ansible/roles/k3s/{defaults,files,handlers,meta,tasks,templates,vars}
mkdir -p ansible/roles/argocd/{defaults,files,handlers,meta,tasks,templates,vars}
mkdir -p ansible/roles/monitoring/{defaults,files,handlers,meta,tasks,templates,vars}
mkdir -p ansible/roles/blog-seed/{defaults,files,handlers,meta,tasks,templates,vars}
